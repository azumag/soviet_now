あなたはパズルゲーム「ソ連パズル」の戦略改善AI。`strategy.py` の `decide()` 関数を改善せよ。

## ゲーム概要
- ソ連パズル（スイカゲーム風）: 同type2個が接触→合体進化 (type_N + type_N → type_{N+1})
- プレイヤーはドロップX座標(-3.0〜+3.0)のみ指定可能
- ゲームオーバー: ピースがデッドライン(y≒2.5)を超える
- 目標: スコア最大化

## strategy.py のインターフェース契約（変更禁止）

```python
def decide(game_state: dict, analysis: dict) -> dict:
    """
    Args:
        game_state: {"state", "score", "next": {"type", "r"}, "nextNext": {"type", "r"},
                     "pieces": [{"id", "type", "x", "y", "r", "vx", "vy", ...}], "shapes": {...}}
        analysis: {"results": [{x, landing_y, merge_grade, score, has_merge, merges, ...}],
                   "same_type": [{id, type, x, y, r}],
                   "reactor": {type_count, reactive_pairs, near_pairs, pipeline, ...}}
    Returns:
        {"x": float, "reason": str}  # x は -3.0〜+3.0
    """
```

## 変更ルール
- `decide()` の**シグネチャ** (`def decide(game_state, analysis) -> dict`) は変更禁止
- `if __name__ == "__main__"` ブロックは変更禁止
- `decide()` 内部、ヘルパー関数、定数、import、モジュールレベル変数は自由に変更可能
- strategy.py のみ変更可能。他のファイルは変更しない
- 変更内容をファイル冒頭のコメントに追記せよ

## 参照データの読み方

以下のデータはこのプロンプトに自動で埋め込まれている。ファイルを自分で読みに行く必要はない。

### strategy.py (現在版)
現在の decide() ロジック。改善対象。

### game_history/latest.jsonl (ターン履歴)
各行がJSON: `{"turn", "score", "score_delta", "piece_count", "max_y", "next_type", "decision_x", "decision_reason", "merge_available", "best_merge_grade", "reactor_reactive_pairs", ...}`


### game_state.json (最終盤面)
ゲームオーバー時の盤面。散在・到達不能ピースの分析に使用。

## ★★★ 最重要0: 同じ改善を繰り返すな ★★★

**改善の第一歩は、過去の変更履歴を読み、自分が堂々巡りしていないか確認することである。**

### 反復検出（必ず最初に実行せよ）

strategy.py 冒頭の `# --- 変更履歴 ---` を全て読み、以下をチェックせよ:

1. **振り子パターン**: 「Aを削除」→「Aを再導入」→「Aを削除」のように同じ要素を追加・削除・再追加していないか？
   - 例: 「旗側管理削除」→「旗側管理再導入」→「旗側管理削除」
   - 例: 「危機回避閾値1.2→1.5」→「危機回避閾値1.5→1.2」
2. **閾値シャッフル**: 同じパラメータの閾値を微調整し続けていないか？（90%→95%→90%→95%...）
3. **名前変え再発明**: 本質的に同じロジックを別の名前で何度も導入していないか？

### 振り子を検出した場合 → ブレイクスルーが必要

過去の履歴で振り子パターンを発見したら、**その要素の追加も削除もするな**。代わりに:

1. **なぜ振り子が起きるか根本原因を考えよ**: 「削除するとスコアが下がる、でも存在すると別の問題が起きる」なら、その要素自体ではなく**周囲の設計が悪い**
2. **第三の選択肢を考えよ**: AかBかではなく、AもBも不要になるような全く別のアプローチを探せ
3. **抽象度を上げよ**: 個別のif分岐ではなく、スコアリング関数の重み付けなど、より一般的な仕組みで解決できないか？
4. **その要素が本当にスコア向上に必要か、履歴データで確認せよ**: 発動時のscore_deltaが正なら有効、0なら無駄

### ブレイクスルーのアイデア例
- 全候補を**単一のスコアリング関数**で統一評価する（マージボーナス・旗側ボーナス・高さペナルティを重み付き合算）→ if分岐の連鎖を排除
- **モンテカルロ的アプローチ**: analysis["results"]のトップN候補を確率的に選択し、局所最適に陥らない
- **フェーズ制**: ピース数や最大typeに応じてゲームフェーズを分け、フェーズごとに完全に異なる戦略を適用
- **過去の自分のdecision_reasonの分布を統計的に分析**し、最もスコア増加に寄与した理由のパターンを強化する

## ★★★ 最重要1: 既存戦略を疑え ★★★

**改善とは閾値の微調整ではない。既存の戦略構造そのものを疑うことである。**

### 改善の優先順位
1. **反復していないか確認が最優先**: 上記の反復検出を必ず最初に行え
2. **構造レベルの戦略改善が次に優先**: ロジックの追加・削除・統合・書き直しなど、戦略の構造を変える改善を第一に考えよ
3. **閾値の微調整は補助手段**: 閾値調整は禁止しないが、それだけで終わらせるな。構造改善と組み合わせて初めて意味がある

### 禁止事項
1. **コピペ追記禁止**: 既存ロジックをそのまま残して新しいif分岐を追加するだけの変更は禁止。既存ロジックの削除・統合・書き直しを恐れるな
2. **ファイル管理はスクリプトが担当**: 戦略バージョンの保存・削除・殿堂入り・変更履歴のトリミングは全てスクリプト側で自動実行される。AIは strategy.py の中身の改善だけに集中せよ

### やるべきこと（構造レベルの改善）
1. **「このロジックは本当に必要か？」を全ブロックに問え**: 特にif分岐が5段以上ネストしているロジック、到達率が低そうな分岐、互いに矛盾する条件は削除候補
2. **decision_reason の分布を分析せよ**: 履歴のdecision_reasonを集計し、最も多い理由が「マージ」以外なら、その戦略が支配的すぎる。支配的な非マージ戦略は弱体化または削除を検討せよ
3. **履歴のdecision_reasonとscore_deltaの相関を分析せよ**: どの理由でドロップした時にスコアが伸びたか？伸びなかったか？データに基づいて戦略を取捨選択せよ
4. **analysis["results"] をもっと活用せよ**: analyze_board.py が計算済みの期待値ランキングを無視して、独自の旗側ロジックや高さ制限で上書きしていないか？ analysis の score が高い選択肢を無視する理由が本当にあるか疑え
5. **ロジックの削除を恐れるな**: 100行のコードを50行に減らしてスコアが上がることはありえる。複雑さ自体がバグの温床。シンプルなロジックのほうが頑健なことが多い
6. **全く新しいアプローチを試せ**: 例えば「analysis["results"]のscoreトップ3から選ぶだけ」というシンプルな戦略がベースラインとして現在より良い可能性がある。まずベースラインを考え、そこに本当に必要な制約だけを足せ

### 改善の思考プロセス
```
1. 履歴分析 → 「何が悪かったか」ではなく「どのロジックが悪さをしたか」を特定
2. そのロジックの存在意義を疑う → 本当に必要？削除したらどうなる？
3. 履歴データで、そのロジック発動時のscore_deltaは正だったか？ → 0以下なら不要
4. ロジックを削除 or 根本的に書き直し → 閾値微調整は最終手段
5. 全体のコード量が減っているか確認 → 減っていなければ複雑さを追加しただけ
```

## 分析→改善の手順

1. **履歴分析**: latest.jsonl を全ターン分析
   - マージ成功率 (merge_available かつ score 増加のターン / 全ターン)
   - スコア停滞パターン (3ターン以上 score_delta=0)
   - max_y の推移 (危機的状況の検出と対処)
   - **decision_reason の頻度分布** ← 最も重要。どの戦略が何回発動したか

2. **最終盤面分析**: game_state.json
   - 大型ピース(type8+)の配置: 旗側集約できていたか
   - 到達不能ペア: 同type同士が離れすぎていないか
   - ピース分布: 左右バランス

3. **構造的問題の特定**: strategy.py のロジック構造を批判的に分析
   - 到達頻度が低い分岐はないか
   - 互いに矛盾・重複する条件はないか
   - analysis["results"] の情報を十分に活用しているか
   - **コード行数は前バージョンより減らせるか**

4. **改善実装**: strategy.py の decide() を改善
   - 不要なロジックの削除・統合
   - analysis["results"] のスコアをより尊重する設計
   - シンプルで頑健なロジックへの書き直し

## 改善のヒント

### 使える情報
- `game_state["pieces"]`: 全ピースの位置・type・速度
- `game_state["next"]`/`game_state["nextNext"]`: 次と次の次のピース
- `analysis["results"]`: 全サンプルX座標の期待値ランキング（analyze_board.py計算済み）
- `analysis["reactor"]`: 反応器状態（同typeペア距離、パイプライン健全性）

### 有効なパターン
- **旗側集約**: type9+ の最初の配置側を記憶し、以後同じ側に配置
- **nextNext保護**: 次の次のピースのマージ経路を塞がない配置
- **高さ制限**: max_y > 1.5 で壁ドロップ禁止
- **シェイク戦略**: マージ不可が続く時、小ピースで下層を揺らす
- **連鎖設計**: typeN-1ペアをtypeNの近くに配置
- **モジュールレベル変数**: 試合内の状態保持（旗側、連続無マージ数、前回ドロップX等）

## 出力

strategy.py を**上書き**で出力せよ。Write ツールで `strategy.py` に書き込むこと。

注意:
- `if __name__ == "__main__"` ブロックは元のまま維持すること
- 冒頭コメントに変更履歴を追記せよ（トリミングはスクリプトが自動で行う）
- `# [BEST:スコア]` タグは触るな（スクリプトが管理する）
- decide() は必ず `{"x": float, "reason": str}` を返すこと
- **コード行数が前バージョンより増えている場合、本当に必要か再考せよ**
